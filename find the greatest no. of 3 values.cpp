#include <iostream>
using namespace std;
main()
{
	int a,b,c;
	cout<<"Enter the value of a : ";
	cin>>a;
	cout<<"Enter the value of b : ";
	cin>>b;
	cout<<"Enter the value of c : ";
	cin>>c;
	 
               	  if (a>=b && a>=c)
             	         cout<<"The greatest number is a : "<<a;
                       	else if (b>=a && b>=c)
                        	cout<<"The greatest number is b : "<<b;
                        	else if (c>=a && c>=b)
                             	cout<<"The greatest number is c : "<<c;s
	
	
}
